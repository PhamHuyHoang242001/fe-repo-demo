import React from 'react';
import PageShell from './PageShell';

// Màn trắng placeholder — Skill Package (route /asset-hub/skill).
const SkillPackage: React.FC = () => <PageShell crumb="Tài sản ứng dụng › Skill" title="Skill Package" />;

export default SkillPackage;
