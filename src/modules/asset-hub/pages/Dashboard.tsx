import React from 'react';
import PageShell from './PageShell';

// Màn trắng placeholder — Dashboard (route index /asset-hub).
const Dashboard: React.FC = () => <PageShell crumb="Tổng quan" title="Control Plane Dashboard" />;

export default Dashboard;
